apiVersion: route.openshift.io/v1
kind: Route
metadata:
  creationTimestamp: null
  name: backend-app
  namespace: stage
spec:
  host: backend-app.stage.svc
  port:
    targetPort: 8080
  to:
    kind: Service
    name: backend-app
    weight: 100
  wildcardPolicy: None